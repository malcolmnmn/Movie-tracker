PORT=4000
JWT_SECRET=change-this-secret-in-production

# Optional: Pfad zur SQLite-Datei, z. B. auf einem persistenten Volume beim Hoster
# DATABASE_PATH=/data/data.sqlite

# Optional: kommagetrennte Liste erlaubter Origins (leer = alle erlaubt)
# CORS_ORIGIN=https://deine-domain.de
